import { DefaultTheme, Provider as PaperProvider } from 'react-native-paper';

const themeApp = {
    ...DefaultTheme,
    colors: {
      ...DefaultTheme.colors,
      primary: '#012a4a',
      accent: '#FF9B54',
    },
    
  };


export default themeApp
